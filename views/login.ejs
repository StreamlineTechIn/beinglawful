<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login - Being Lawful</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet"/>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f8f9fa;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        .login-container {
            max-width: 500px;
            width: 100%;
            background-color: #fff;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
        }
        .login-container h2 {
            text-align: center;
            margin-bottom: 30px;
            color: #2c3e50;
        }
        .nav-tabs .nav-link {
            color: #28a745;
            font-weight: 600;
        }
        .nav-tabs .nav-link.active {
            color: #218838;
            border-color: #28a745;
        }
        .form-label {
            font-weight: 500;
            color: #2c3e50;
        }
        .btn-custom {
            width: 100%;
            padding: 12px;
            background-color: #28a745;
            color: #fff;
            border: none;
            border-radius: 5px;
            font-weight: 600;
            cursor: pointer;
        }
        .btn-custom:hover {
            background-color: #218838;
        }
        .error-message {
            color: #dc3545;
            text-align: center;
            margin-bottom: 15px;
        }
    </style>
</head>
<body>
    <div class="login-container">
        <h2>Login to Being Lawful</h2>

        <% if (error) { %>
            <div class="error-message"><%= error %></div>
        <% } %>

        <ul class="nav nav-tabs mb-4" id="loginTabs" role="tablist">
            <li class="nav-item" role="presentation">
                <button class="nav-link active" id="student-tab" data-bs-toggle="tab" data-bs-target="#student-login" type="button" role="tab" aria-controls="student-login" aria-selected="true">Student</button>
            </li>
            <li class="nav-item" role="presentation">
                <button class="nav-link" id="school-tab" data-bs-toggle="tab" data-bs-target="#school-login" type="button" role="tab" aria-controls="school-login" aria-selected="false">School</button>
            </li>
        </ul>

        <div class="tab-content" id="loginTabsContent">
            <div class="tab-pane fade show active" id="student-login" role="tabpanel" aria-labelledby="student-tab">
                <form action="/student-login" method="POST">
                    <div class="mb-3">
                        <label for="student-username" class="form-label">Username (Parent Mobile 1)</label>
                        <input type="text" class="form-control" id="student-username" name="username" required>
                    </div>
                    <div class="mb-3">
                        <label for="student-password" class="form-label">Password (DDMMYYYY of Birthdate)</label>
                        <input type="password" class="form-control" id="student-password" name="password" required>
                    </div>
                    <button type="submit" class="btn-custom">Login as Student</button>
                </form>
            </div>

            <div class="tab-pane fade" id="school-login" role="tabpanel" aria-labelledby="school-tab">
                <form action="/school-login" method="POST">
                    <div class="mb-3">
                        <label for="school-username" class="form-label">Username (School Email)</label>
                        <input type="email" class="form-control" id="school-username" name="username" required>
                    </div>
                    <div class="mb-3">
                        <label for="school-password" class="form-label">Password (Principal Number)</label>
                        <input type="password" class="form-control" id="school-password" name="password" required>
                    </div>
                    <button type="submit" class="btn-custom">Login as School</button>
                </form>
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>