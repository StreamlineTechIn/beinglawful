<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>School Participation Form</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet"/>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background-color: #f8f9fa;
            padding: 20px;
        }
        .form-container, .schools-list {
            max-width: 800px;
            margin: 0 auto;
            background-color: #fff;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .form-container h2, .schools-list h2 {
            text-align: center;
            margin-bottom: 20px;
        }
        .form-group {
            margin-bottom: 15px;
        }
        .form-group label {
            font-weight: 500;
        }
        .form-group input {
            width: 100%;
            padding: 8px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }
        .btn-submit {
            display: block;
            width: 100%;
            padding: 10px;
            background-color: #007bff;
            color: #fff;
            border: none;
            border-radius: 5px;
            font-weight: 600;
            cursor: pointer;
        }
        .btn-submit:hover {
            background-color: #0056b3;
        }
        .schools-list ul {
            list-style: none;
            padding: 0;
        }
        .schools-list li {
            background-color: #e0e0e0;
            padding: 10px;
            margin-bottom: 10px;
            border-radius: 5px;
        }
    </style>
</head>
<body>
    <div class="form-container">
        <h2>School Participation Form</h2>
        <form action="/school-participate" method="POST">
            <div class="form-group">
                <label for="schoolName">School Name</label>
                <input type="text" id="schoolName" name="schoolName" required>
            </div>
            <div class="form-group">
                <label for="city">City</label>
                <input type="text" id="city" name="city" required>
            </div>
            <div class="form-group">
                <label for="district">District</label>
                <input type="text" id="district" name="district" required>
            </div>
            <div class="form-group">
                <label for="pincode">Pincode (6 digits)</label>
                <input type="text" id="pincode" name="pincode" pattern="[0-9]{6}" placeholder="Enter 6-digit pincode" required>
            </div>
            <div class="form-group">
                <label for="schoolPhoneNumber">School Phone Number</label>
                <input type="tel" id="schoolPhoneNumber" name="schoolPhoneNumber" pattern="[0-9]{10}" placeholder="Enter 10-digit number" required>
            </div>
            <div class="form-group">
                <label for="schoolEmail">School Email</label>
                <input type="email" id="schoolEmail" name="schoolEmail" placeholder="Enter school email" required>
            </div>
            <div class="form-group">
                <label for="principalNumber">Principal's Mobile Number</label>
                <input type="tel" id="principalNumber" name="principalNumber" pattern="[0-9]{10}" placeholder="Enter 10-digit number" required>
            </div>
            <div class="form-group">
                <label for="principalEmail">Principal's Email</label>
                <input type="email" id="principalEmail" name="principalEmail" placeholder="Enter principal's email" required>
            </div>
            <div class="form-group">
                <label for="civicsTeacherNumber">Civics Teacher's Mobile Number</label>
                <input type="tel" id="civicsTeacherNumber" name="civicsTeacherNumber" pattern="[0-9]{10}" placeholder="Enter 10-digit number" required>
            </div>
            <div class="form-group">
                <label for="civicsTeacherEmail">Civics Teacher's Email</label>
                <input type="email" id="civicsTeacherEmail" name="civicsTeacherEmail" placeholder="Enter civics teacher's email" required>
            </div>
            <button type="submit" class="btn-submit">Submit</button>
        </form>
    </div>

    <div class="schools-list">
        <h2>Participating Schools</h2>
        <% if (schools.length > 0) { %>
            <ul>
                <% schools.forEach(school => { %>
                    <li>
                        <strong>School Name:</strong> <%= school.schoolName %><br>
                        <strong>District:</strong> <%= school.district %><br>
                                         </li>
                <% }) %>
            </ul>
        <% } else { %>
            <p>No schools have participated yet.</p>
        <% } %>
    </div>
</body>
</html>