<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Trainer Dashboard - Being Lawful</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
            display: flex;
            min-height: 100vh;
        }
        .sidebar {
            width: 250px;
            background-color: #2c3e50;
            color: white;
            padding: 20px 0;
            position: fixed;
            height: 100%;
            box-shadow: 2px 0 5px rgba(0,0,0,0.1);
        }
        .sidebar h2 {
            padding: 10px 20px;
            margin: 0;
            font-size: 1.5em;
        }
        .sidebar ul {
            list-style: none;
            padding: 0;
            margin: 0;
        }
        .sidebar ul li {
            padding: 15px 20px;
        }
        .sidebar ul li a {
            color: #3498db;
            text-decoration: none;
            font-size: 1.1em;
        }
        .sidebar ul li a:hover {
            color: #e74c3c;
            text-decoration: underline;
        }
        .content {
            margin-left: 270px;
            padding: 20px;
            width: calc(100% - 270px);
            background: white;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
        }
        h1 {
            color: #333;
            margin-top: 0;
            font-size: 2em;
        }
        .section {
            margin-bottom: 30px;
        }
        .form-group {
            margin-bottom: 15px;
        }
        label {
            display: block;
            font-weight: bold;
            margin-bottom: 5px;
        }
        input[type="date"] {
            padding: 8px;
            width: 250px;
            border: 1px solid #ccc;
            border-radius: 4px;
            box-sizing: border-box;
        }
        button {
            padding: 10px 20px;
            background-color: #007bff;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-size: 1em;
            margin-right: 10px;
        }
        button:hover {
            background-color: #0056b3;
        }
        a {
            color: #007bff;
            text-decoration: none;
        }
        a:hover {
            text-decoration: underline;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            background-color: #fff;
            margin-top: 20px;
        }
        th, td {
            padding: 12px;
            text-align: left;
            border-bottom: 1px solid #ddd;
        }
        th {
            background-color: #2c3e50;
            color: white;
            font-size: 1.1em;
        }
        tr:nth-child(even) {
            background-color: #f8f9fa;
        }
        tr:hover {
            background-color: #e9ecef;
        }
        .delete-btn {
            background-color: #dc3545;
            padding: 5px 10px;
            border: none;
            border-radius: 4px;
            color: white;
            cursor: pointer;
        }
        .delete-btn:hover {
            background-color: #c82333;
        }
        @media (max-width: 768px) {
            .sidebar {
                width: 100%;
                height: auto;
                position: relative;
            }
            .content {
                margin-left: 0;
                width: 100%;
            }
            input[type="date"] {
                width: 100%;
            }
        }
    </style>
</head>
<body>
    <div class="sidebar">
        <h2>Trainer Portal</h2>
        <ul>
            <li><a href="/trainer-dashboard?username=<%= email %>">Dashboard</a></li>
            <li><a href="/trainer-dashboard?username=<%= email %>#assigned-schools">Assigned Schools</a></li>
            <li><a href="/trainer-gamezone">Gamezone</a></li>
            <li><a href="/logout">Logout</a></li>
        </ul>
    </div>
    <div class="content">
        <h1>Welcome, <%= trainerName %></h1>
        <p><strong>Email:</strong> <%= email %></p>
        <p><strong>City:</strong> <%= city %></p>
        <p><strong>District:</strong> <%= district %></p>
        <p><strong>Profession:</strong> <%= profession %></p>

        <% if (error) { %>
            <p style="color: red;"><%= error %></p>
        <% } %>
        <% if (success) { %>
            <p style="color: green;"><%= success %></p>
        <% } %>

        <% if (trainerData && trainerData.isApproved) { %>
            <p style="color: green;">Congratulations! You have completed the workshop.</p>
            <div class="section">
                <h2>Add Available Dates</h2>
                <form action="/trainer-dashboard/add-dates?username=<%= email %>" method="POST">
                    <input type="hidden" name="trainerEmail" value="<%= email %>">
                    <div class="form-group">
                        <label for="availableDate1">Available Date 1:</label>
                        <input type="date" id="availableDate1" name="availableDates" required>
                    </div>
                    <div class="form-group">
                        <label for="availableDate2">Available Date 2 (optional):</label>
                        <input type="date" id="availableDate2" name="availableDates">
                    </div>
                    <button type="submit">Add Dates</button>
                </form>

                <h3>Your Available Dates</h3>
                <% if (availableDates.length > 0) { %>
                    <table>
                        <tr>
                            <th>Date</th>
                            <th>Action</th>
                        </tr>
                        <% availableDates.forEach(date => { %>
                            <tr>
                                <td><%= date %></td>
                                <td><form action="/trainer-dashboard/delete-date?username=<%= email %>" method="POST" style="display:inline;">
                                    <input type="hidden" name="dateToDelete" value="<%= date %>">
                                    <button type="submit" class="delete-btn">Delete</button>
                                </form></td>
                            </tr>
                        <% }) %>
                    </table>
                <% } else { %>
                    <p>No available dates added yet.</p>
                <% } %>
            </div>
        <% } else { %>
            <div class="section">
                <p>You are not completed the Train the Trainer yet!</p>
            </div>
        <% } %>

        <div class="section" id="assigned-schools">
            <h2>Assigned Schools</h2>
            <% if (assignedSchools.length > 0) { %>
                <table>
                    <tr>
                        <th>School Name</th>
                        <th>City</th>
                        <th>District</th>
                        <th>Event Date</th>
                        <th>Resources Confirmed</th>
                        <th>Selected Resources</th>
                    </tr>
                    <% assignedSchools.forEach(school => { %>
                        <tr>
                            <td><%= school.schoolName %></td>
                            <td><%= school.city %></td>
                            <td><%= school.district %></td>
                            <td><%= school.eventDate %></td>
                            <td><%= school.resourcesConfirmed ? 'Yes' : 'No' %></td>
                            <td><%= school.selectedResources.join(', ') || 'None' %></td>
                        </tr>
                    <% }) %>
                </table>
            <% } else { %>
                <p>No schools assigned yet.</p>
            <% } %>
        </div>

        <div class="section">
            <h2>Schools in Your District (<%= district %>)</h2>
            <% if (schoolsInDistrict.length > 0) { %>
                <table>
                    <tr>
                        <th>School Name</th>
                        <th>Email</th>
                        <th>City</th>
                        <th>Event Date</th>
                        <th>Assigned Trainer</th>
                    </tr>
                    <% schoolsInDistrict.forEach(school => { %>
                        <tr>
                            <td><%= school.schoolName %></td>
                            <td><%= school.schoolEmail %></td>
                            <td><%= school.city %></td>
                            <td><%= school.eventDate %></td>
                            <td><%= school.assignedTrainerId ? 'Assigned' : 'Not Assigned' %></td>
                        </tr>
                    <% }) %>
                </table>
            <% } else { %>
                <p>No schools found in your district.</p>
            <% } %>
        </div>
    </div>
</body>
</html>