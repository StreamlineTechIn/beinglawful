<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MCQ Results - Being Lawful</title>
    <style>
        body { font-family: Arial, sans-serif; background: #f4f4f4; padding: 20px; }
        .result-container { max-width: 800px; margin: auto; background: white; padding: 20px; border-radius: 8px; }
        .question { margin-bottom: 15px; }
        .correct { color: green; }
        .wrong { color: red; }
    </style>
</head>
<body>
    <div class="result-container">
        <h2>MCQ Exam Results</h2>
        <% result.forEach((item, index) => { %>
            <div class="question">
                <p><%= index + 1 %>. <%= item.question %></p>
                <p>Your Answer: 
                    <strong class="<%= item.isCorrect ? 'correct' : 'wrong' %>">
                        <%= item.submittedAnswer || 'Not Answered' %>
                    </strong>
                </p>
                <% if (!item.isCorrect) { %>
                    <p>Correct Answer: <strong class="correct"><%= item.correctAnswer %></strong></p>
                <% } %>
                <hr>
            </div>
        <% }) %>
    </div>
</body>
</html>
